# this creates a virtual environment to install external packages
python3 -m venv [insert name of environment]
# to activate
python3 -m venv [insert name of environment]
# this checks what packages are installed
pip3 freeze > requirements.txt